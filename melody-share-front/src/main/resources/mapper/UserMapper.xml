<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpujazz.front.mapper.UserMapper">
    <insert id="insertUser">
        insert into users(`username`, password, nickname, avatar, role, status, created_at, updated_at) VALUES
                         (#{username}, #{password}, #{nickname}, #{avatar}, #{role}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectUserByUsername" resultType="com.cpujazz.front.pojo.entity.User">
        select * from users where username=#{username}
    </select>

</mapper>